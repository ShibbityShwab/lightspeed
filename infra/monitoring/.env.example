# Grafana admin credentials — required (compose refuses to start without these).
# Copy this file to `.env` and choose a strong password:
#   cp .env.example .env
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me-strong-password
